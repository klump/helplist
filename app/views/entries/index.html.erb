<h1>Helplist for all courses</h1>

<p><span class="label label-info">Tip</span> Append the course to the URL to filter for a specific course, e.g. <%= link_to filter_entries_url("it334g"), filter_entries_path("it334g") %></p>

<%= render "helplist" %>
